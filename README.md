# Gilbert Portfolio (Nuxt 4)

Personal portfolio rebuilt from a Nuxt template and populated with content migrated from the previous Nuxt 3 portfolio.

## Development

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Preview production build:

```bash
npm run preview
```
